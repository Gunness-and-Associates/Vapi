# ── HQ Lead Engine · Voice Agent (Sofia) · Phase 1 prototype ──
# Copy this file to ".env" and paste your keys.
# Using your EXISTING subscriptions: Deepgram + OpenAI + ElevenLabs.

# Speech-to-text — Deepgram (existing)
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-3

# LLM — OpenAI (existing).  Use a FAST model — NOT gpt-5 (too slow for live voice).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Text-to-speech — ElevenLabs (existing).  Reusing Sofia's tuned "Laura" voice.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=FGY2WhTYpPnrIDTdsKH5
ELEVENLABS_MODEL=eleven_flash_v2_5
