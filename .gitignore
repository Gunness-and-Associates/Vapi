# ── Secrets — NEVER commit ──
.env
**/.env
*.key
*.pem

# ── Python ──
__pycache__/
**/__pycache__/
*.py[cod]
.venv/
**/.venv/
venv/
env/

# ── Logs ──
*.log
**/*.log

# ── Caches ──
.ruff_cache/
**/.ruff_cache/
.pytest_cache/
.mypy_cache/

# ── Runtime data — may contain real customer PII, NEVER commit to a public repo ──
# call transcripts / outcomes, and the embedded knowledge-base index + uploads
**/calls.json
**/knowledge_index.json
**/knowledge/
!**/knowledge/.gitkeep

# ── Internal strategy docs — excluded from the PUBLIC repo ──
# (business costs/margins/strategy — add back only if the repo is made private)
docs/

# ── OS ──
Thumbs.db
.DS_Store
